docker-compose run django django-admin startproject core .
docker exec -it database psql -U postgres

//     command: sh -c "cd /usr/src/app; pip3 install -r requirements.txt; python3 manage.py migrate app; python3 manage.py migrate; python3 manage.py runserver 0.0.0.0:8000"

// -------------
// LOKI-driver -
// -------------
// install loki driver for docker (not supported on windows)
docker plugin install grafana/loki-docker-driver:latest --alias loki --grant-all-permissions

// check if plugin installed
docker plugin ls

// upgrading loki-driver
docker plugin disable loki --force
docker plugin upgrade loki grafana/loki-docker-driver:latest --grant-all-permissions
docker plugin enable loki
systemctl restart docker

// uninstall
docker plugin disable loki --force
docker plugin rm loki

