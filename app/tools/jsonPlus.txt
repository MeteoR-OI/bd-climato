from app.tools.jsonPlus import JsonPlus
import json
jp = JsonPlus()
z = jp.json_test()
jj=jp.loads(z)
# check that all dat/last_dat_rec/max_out_temp_time was well decoded as datetime -> isinstance(jj['data'][0]['current']['dat'], datetime.datetime) ==  True

jj2 = jp.loads(jp.dumps(jj))
# Check que jj2 a toutes les memes cles/valeur que j -> jp.dumps(jj2) == jp.dumps(jj)

js = jp.serialize(jj)
# check that all dat/last_dat_rec/max_out_temp_time was well encoded as string -> isinstance(js['data'][0]['current']['dat'], str) ==  True

je = jp.deserialize(js)
# check that all dat/last_dat_rec/max_out_temp_time was well decoded as datetime -> isinstance(je['data'][0]['current']['dat'], datetime.datetime) ==  True



